/**
 * User in touching A button on the right controller.
 * @type {number}
 */
export const A = 1000;

/**
 * User in touching B button on the right controller.
 * @type {number}
 */
export const B = 1001;

/**
 * User has a finger on the thumb stick of the right controller.
 * @type {number}
 */
export const R_THUMB = 1002;

/**
 * User has a finger on the textured thumb rest of the right controller.
 * @type {number}
 */
export const R_THUMB_REST = 1003;

/**
 * User in touching the index finger trigger on the right controller.
 * @type {number}
 */
export const R_INDEX_TRIGGER = 1004;

/**
 * User in touching X button on the left controller.
 * @type {number}
 */
export const X = 1005;

/**
 * User in touching Y button on the left controller.
 * @type {number}
 */
export const Y = 1006;

/**
 * User has a finger on the thumb stick of the left controller.
 * @type {number}
 */
export const L_THUMB = 1007;

/**
 * User has a finger on the textured thumb rest of the left controller.
 * @type {number}
 */
export const L_THUMB_REST = 1008;

/**
 * User in touching the index finger trigger on the left controller.
 * @type {number}
 */
export const L_INDEX_TRIGGER = 1009;

/**
 * Users right index finger is pointing forward past the trigger.
 * @type {number}
 */
export const R_INDEX_POINTING = 1010;

/**
 * Users right thumb is up and away from buttons on the controller, a gesture that can be interpreted as right thumbs up.
 * @type {number}
 */
export const R_THUMB_UP = 1011;

/**
 * Users left index finger is pointing forward past the trigger.
 * @type {number}
 */
export const L_INDEX_POINTING = 1012;

/**
 * Users left thumb is up and away from buttons on the controller, a gesture that can be interpreted as left thumbs up.
 * @type {number}
 */
export const L_THUMB_UP = 1013;

/**
 Enter button on the left Touch controller. This is equivalent to the Start button on the Xbox controller.
 @type {number}
 */
export const ENTER = 1015;